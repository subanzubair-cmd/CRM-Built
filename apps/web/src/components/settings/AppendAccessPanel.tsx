'use client'

/**
 * AppendAccessPanel
 *
 * Re-exports the RoleAssignmentPanel which implements the full
 * multi-column append access flow (Roles, Lead Access/Markets, Campaigns).
 * This exists as a named alias so the component tree matches the spec.
 */
export { RoleAssignmentPanel as AppendAccessPanel } from './RoleAssignmentPanel'
